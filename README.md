# AI Image Detector 🖼️🤖

A full-stack machine learning application that detects whether an uploaded image is **AI-generated or real**.  
The model is trained on a **GenImage-derived dataset**, making it significantly more reliable on **real-world photos** (e.g. phone images) compared to benchmark-only datasets.

## 🚀 Features
- Upload any image and receive:
  - **Prediction**: AI-generated or Real
  - **Confidence score**
- Patch-based inference with multiple crops for more stable predictions
- Clean web UI for easy testing
- FastAPI backend with a modern ML inference pipeline
